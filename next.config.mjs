/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: [
            'img.icons8.com',
            'icons8.com',
            'encrypted-tbn0.gstatic.com',
            'upload.wikimedia.org'
        ]
    }
};

export default nextConfig;
